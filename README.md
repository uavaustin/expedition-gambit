
# Expedition Gambit

> A telemetry dashboard for UAVA.

### Usage
* `yarn install`
* `yarn build:proto`
* `yarn start`
* Edit the default config by clicking the ⚙️ icon

```TODO Screenshot```

### Dev Usage
* `yarn install`
* `yarn build:proto`
* `yarn dev`